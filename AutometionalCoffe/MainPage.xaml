<Page
    x:Class="AutometionalCoffee.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AutometionalCoffee" xmlns:view="using:AutometionalCoffe.View" xmlns:local1="using:AutometionalCoffe.View.Control"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
            <!--Coffee coonfig control-->
        <local1:UserDisplayControl 
                x:Name="UserDisplay"
                Grid.Column="1"   
                ViewModel="{x:Bind ViewModel.UserDisplayViewModel}"/>

        <view:CoffeeWorkView x:Name="CoffeeWork"
                             ViewModel="{x:Bind ViewModel.CoffeeWorkViewModel}"
                             Grid.Column="0"/> 
    </Grid>
</Page>
